# Tests

Test fixtures contain realistic secret patterns for detection validation.
Run tests locally: `python -m unittest tests.test_scanner -v`

Tests are maintained in the private development repository and are not
published to GitHub due to push protection constraints (the test fixtures
intentionally contain patterns that look like real API keys).
